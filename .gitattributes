# Default: normalize all text files to LF
* text=auto eol=lf

# Python files (required for Linux / Docker)
*.py text eol=lf

# Shell scripts (entrypoints, cron scripts)
*.sh text eol=lf
deploy/*.sh text eol=lf
cron/* text eol=lf

# Cron configuration files
deploy/mycron text eol=lf
cron/2fa-cron text eol=lf

# PEM key files (MUST stay LF, CRLF breaks crypto)
*.pem text eol=lf

# Dockerfile must use LF
Dockerfile text eol=lf

# YAML files (docker-compose.yml)
*.yml text eol=lf
*.yaml text eol=lf
